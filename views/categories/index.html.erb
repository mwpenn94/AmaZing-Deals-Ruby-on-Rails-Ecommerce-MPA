<h1> Shop by category </h1>
<% if current_user.support? %>
    <p><strong><%= link_to "New category", new_category_path(@category) %></strong></p>
<% end %>
  <div>
<% @categories.each do |category| %>
    <div>
        <h2><%= link_to category.name, category_path(category) %></h2>
    </div>
<% end %>